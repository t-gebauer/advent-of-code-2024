;; Executables can not have tests.

(library
  (name lib)
  (modules lib)
  (inline_tests)
  (preprocess (pps ppx_expect)))

(library
  (name days)
  (modules
    day01
    day02
    day03)
  (libraries str lib)
  (inline_tests)
  (preprocess (pps ppx_expect ppx_deriving.show)))

(executable
  (name runner)
  (modules runner)
  (libraries days))
